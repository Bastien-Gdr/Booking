<nav class="navbar navbar-expand-lg navbar-light bg-info">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('homepage')}}">AirB&B</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarColor03">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link active" href="{{path('admin_dashboard')}}"><i class="fas fa-chart-line m-2"></i>Tableau de bord</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('admin_ads_list')}}"><i class="fas fa-hotel m-2"></i>Annonces</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('admin_bookings_list')}}"><i class="fas fa-folder m-2"></i>Réservations</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('admin_comments_list')}}"><i class="fas fa-comments m-2"></i>Commentaires</a>
				</li>
				<li class="nav-item">
					<a class="nav-link active" href="{{path('admin_users_list')}}"><i class="fa fa-users m-2"></i>Utilisateurs</a>
				</li>
			</ul>
			<ul class="navbar-nav ml-auto align-items-center">

				{% if app.user %}

				<li class="nav-item">
					<a href="{{path('homepage')}}" class="nav-link" target="_blank"><i class="fas fa-eye"></i>Voir le site</a>
				</li>

					<div class="dropdown">
						<a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="accountDropdownLink" data-bs-toggle="dropdown" aria-expanded="false">
							<img src="{{ app.user.avatar }}" alt="Avatar de {{ app.user.fullName }}" class="avatar avatar-small">
							{{app.user.fullName}}
						</a>

						<ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
							<li>
								<a class="dropdown-item" href="{{path('admin_account_logout')}}">Déconnexion</a>
							</li>
						</ul>
					</div>


				{% else %}

					<li class="nav-item">
						<a class="nav-link" href="{{path('account_register')}}">Inscription</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{path('account_login')}}">Connexion</a>
					</li>

				{% endif %}
			</ul>

		</div>
	</div>
</nav>
