{% extends 'base.html.twig' %}

{% block title %}
	Modifications du profil
{% endblock %}

{% block body %}

	<div class="container py-5 mt-5">
		<h1>Modifications du profil</h1>

        {{form_start(form)}}

        {{form_widget(form)}}

        <button type="submit" class="btn btn-dark">Enregistrer</button>
        <a href="{{path('account_home')}}" class="btn btn-warning">Retour</a>
        
        {{form_end(form)}}

	</div>

{% endblock %}
